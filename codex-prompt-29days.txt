PURSUE GOAL: Build a complete LeadRescuePro operations dashboard to close 10 plumbing clients in 29 days. The dashboard manages the FULL LIFECYCLE in one place: cold callers → closers → fulfillment. Everything at /api/ endpoints.

I have a working FastAPI backend at ~/leadrescuepro_ops/dashboard_backend/ on port 8650 with SQLite. Admin login: admin / PIN 4791. Tunnel URL is at the top of admin.html (window.LRP_DASHBOARD_CONFIG.DASHBOARD_TUNNEL_URL) — UPDATE IT TO: https://museum-morgan-cooperation-relatively.trycloudflare.com

DO NOT touch main.py, database.py, or auth.py. Only edit static/admin.html and static/caller.html.

EXISTING:
- static/admin.html: Login + 6 tabs (Overview, Prospects, Callers, Commissions, Weekly, Log Call). Dark cyberpunk theme. Uses req() for API calls.
- static/caller.html: PIN login, quick call log form (business name + phone + disposition + duration), today's stats, call history feed, prospect dropdown.

BACKEND ENDPOINTS ALREADY WORKING:
- GET/POST/PATCH/DELETE /api/prospects — business_name, phone, city, state, score, status, source, loom_sent, assigned_to, notes
- POST /api/calls/log — business_name, phone, disposition (voicemail/no_answer/talked_interested/talked_not_interested/callback_set/dnc/wrong_number), duration_seconds, notes
- GET /api/calls/today — today's calls for logged-in user
- GET /api/kpis/dashboard — dials_today, connects_today, dnc_today, connect_rate, callers_online, top_callers, recent_activity
- GET /api/kpis/weekly — last 7 days of dials/connects/dnc
- GET /api/kpis/caller/{id} — per-caller today stats
- GET /api/kpis/daily-summary — text summary of today
- GET/POST/PATCH/DELETE /api/callers — manage caller accounts (username, full_name, pin, active)
- GET /api/callers/{id}/performance — all-time stats per caller
- GET/POST/PATCH/DELETE /api/commissions — track $100/deal commission
- GET /api/commissions/summary — total per caller
- GET/POST/PATCH/DELETE /api/clients — business_name, owner_name, phone, email, city, state, onboarding_stage (new/call_handling_setup/voicemail_config/calendar_sync/live_testing/active), subscription_status (active/cancelled/paused/trial/churned), setup_fee ($997), monthly_revenue ($499), usage_minutes, usage_rate, started_at, notes
- GET/POST/PATCH/DELETE /api/sdr-candidates — name, email, phone, country, stage (applied/shortlisted/interviewed/training/active/rejected), source (facebook/upwork/linkedin/referral/manual/other)
- POST /api/recordings/upload — upload call recording .webm/.mp3 files

BUILD IN EXACT ORDER — DO NOT STOP UNTIL ALL DONE:

=== 1. ADD 4 NEW TABS TO admin.html ===

Current tabs: Overview, Prospects, Callers, Commissions, Weekly, Log Call
Add: "Pipeline", "Clients & Fulfillment", "SDR Hiring", "Call Recordings"

Tab: PIPELINE (visual funnel, 3 stages)
- Stage 1 LEADS: GET /api/prospects?status=pending — big number + progress bar toward target 1000
- Stage 2 CALLS: GET /api/kpis/dashboard — show today_dials / connects / interested / callbacks. Each as mini card with number. Target: 200 dials
- Stage 3 CLIENTS: GET /api/clients — total active clients. Also show this week's new clients count. Target: 10 total
- Use horizontal progress bars for each stage toward target

Tab: CLIENTS & FULFILLMENT
- GET /api/clients returns all signed clients
- Table: Company, Owner, Phone, Onboarding Stage (colored pill), Status (colored pill), Monthly Rev, Setup Fee, Started
- Revenue summary cards at top: "Total MRR: $X", "Total Setup Collected: $X", "Active Clients: X", "Avg Revenue/Client: $X"
- Fulfillment tracking: For each client, show where they are in setup pipeline:
  Stage 1: New → Stage 2: Call Handling Setup → Stage 3: Voicemail Config → Stage 4: Calendar Sync → Stage 5: Live Testing → Stage 6: Active
  Each client row shows their current onboarding_stage as clickable pills. Click moves them to next stage (PATCH /api/clients/{id} with {"onboarding_stage": "next_stage"})
- Add Client button: opens inline form with business_name, owner_name, phone, email, city, state
- Click company name to edit notes (textarea that patches to /api/clients/{id} with {"notes": "..."})
- Edit subscription_status dropdown (active/paused/cancelled/trial/churned)

Tab: SDR HIRING
- GET/POST/PATCH/DELETE /api/sdr-candidates
- Table: Name, Email, Country, Stage (colored pill), Source, Applied Date
- Add Candidate form at top: name, email, phone, country dropdown (Bangladesh/Philippines/Kenya/India/South Africa/Jamaica/Other), source dropdown
- Stage progress buttons per row: [Applied] → [Shortlisted] → [Interviewed] → [Training] → [Active]. Each button sends PATCH with {"stage": "next_stage"}
- [Reject] button sends PATCH with {"stage": "rejected"} — turns row red
- Notes column: click to edit notes inline
- Show total candidates and how many reached each stage

Tab: CALL RECORDINGS
- GET /api/calls endpoint with date filters
- This tab shows a list of all calls with recording_path (if exists)
- Table: Time, Business, Caller, Disposition, Duration, Recording (play button if recording exists), Transcribe button
- Play button: if recording_path exists, show a playable audio player (use HTML5 <audio> tag)
- Transcribe button: POST /api/calls/{id}/transcribe — shows transcript below row
- Filter by date (today/week/month/all) and by caller dropdown
- Export button: GET /api/calls/export?date=today — downloads as CSV

=== 2. FIX PROSPECTS TAB ===
- Add "Loom" column: button shows "Send Loom" or "Loom Sent ✓" based on prospect.loom_sent
- Clicking "Send Loom" → PATCH /api/prospects/{id} with {"loom_sent": true}
- Add "Assigned To" column: GET /api/callers first, map caller_id to caller name
- Add filter dropdown for assigned_to

=== 3. ENHANCE caller.html ===
- Callback Reminders section: GET /api/prospects?status=callback_set&assigned={caller_id}
  Display as list: Business Name, Phone, Days Since Last Contact (highlight red if >3 days), "Call Now" button
  Sort by last_contact ascending
- Scripts & SOP modal button in header:
  Modal opens with full text of these cold call scripts (include verbatim):
  
  OWNER ANSWERS: "Hi [Name], this is [Caller] from LeadRescuePro. I'll be brief — I help plumbing businesses reduce missed-call leakage. Are you the right person to ask how after-hours and overflow calls are handled?"
  
  GATEKEEPER: "Hi, I'm not calling with an emergency. I'm trying to find who manages call handling or dispatch workflow for [Company]. Who would be best to ask about missed calls and after-hours intake?"
  
  FOLLOW-UP: "Hi [Name], I'm following up on my note about missed-call intake. The only reason I reached out is that plumbing callers often call the next company if voicemail picks up. Worth a 2-minute compare of your current process?"
  
  After each call, these 3 questions:
  1. "About how often do calls hit voicemail during the week?"
  2. "Who decides if a call is urgent enough for immediate dispatch?"
  3. Close: "If you want, I can show you the missed-call intake flow we use — if it's not relevant, no problem."
  
  OBJECTIONS:
  - "Just email info" → "The video I recorded pulls up your specific website to point out the gap — it's tailored for you. What email should I send it to?"
  - "Is this AI?" → "It's conversational voice tech that breathes and pauses like a real person — that's exactly why I want you to hear it yourself in the video."
  - "We don't miss calls" → "The video focuses on what happens at 8PM or during peak hours when lines are tied up. Just a 3-minute breakdown."
  - "No time" → "That's why I'm not asking for a meeting — just dropping a link for when you're free. What email?"

- Auto-refresh every 30 seconds (stats + callback reminders)

=== 4. AUTO-REFRESH admin.html ===
- Overview tab refreshes every 60 seconds
- Call Recordings tab refreshes every 30 seconds

=== CRITICAL ===
- Dark cyberpunk theme: bg=#02040a, panel=#07101b, line=#173653, text=#e8f7ff, cyan=#31d7ff, green=#2dff9a, amber=#ffd166, red=#ff416d
- Mobile-first: 44px min tap targets, horizontal scroll tables, responsive grid
- All API calls use existing req() function pattern
- Do NOT change backend files
- After each change: curl -s http://127.0.0.1:8650/ should return 200 with HTML
- Push to git when done: cd ~/leadrescuepro_ops/dashboard_backend && git add static/ && git commit -m "dashboard: full lifecycle pipeline" && git push

DO NOT STOP until admin.html has 10 tabs total (including the 4 new ones), caller.html has callback reminders + script modal, and curl tests confirm both pages load 200.